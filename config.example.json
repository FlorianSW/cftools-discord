{
  "servers": [{
    "name": "example-server",
    "serverApiId": "CFTools Cloud Server ID",
    "commandMapping": {
      "hasPriority": "hasPriority",
      "leaderboard": "leaderboard"
    }
  }],
  "discordToken": "Discord app token",
  "cftools": {
    "applicationId": "CFTools Cloud application ID",
    "secret": "CFTools Cloud application secret"
  }
}
